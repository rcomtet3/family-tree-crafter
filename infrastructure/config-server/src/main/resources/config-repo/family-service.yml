server:
  port: 8083

spring:
  application:
    name: family-service
  datasource:
    url: jdbc:postgresql://localhost:5432/familytree_family
    username: postgres
    password: postgres

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
