server:
  port: 8082

spring:
  application:
    name: person-service
  datasource:
    url: jdbc:postgresql://localhost:5432/familytree_person
    username: postgres
    password: postgres

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
